<Target Name="PostBuild" AfterTargets="PostBuildEvent">
	<Exec Command="powershell -Command &quot;Write-Host 'Copying DLL to Mods folder...'; Copy-Item -Path '$(TargetPath)' -Destination 'F:\Program Files (x86)\Steam\steamapps\common\Pantheon Rise of the Fallen\Mods' -Force -Verbose; if ($LASTEXITCODE -ne 0) { Write-Host 'Error copying file'; exit 1 } else { Write-Host 'File copied successfully' }&quot;" />
</Target> 